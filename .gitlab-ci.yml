image: ibmcom/cmake
stages:
  - build
  - test
  - deploy

build_job:
  stage: build
  tags:
    - aws 
  script:
    - echo $CI_JOB_NAME
    - cd build
    - cmake ..
    - make
    
deploy_job:
  stage: deploy
  tags:
    - aws
  script:
    - echo $CI_JOB_NAME
     
