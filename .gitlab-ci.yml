image: registry.gitlab.com/opusdv/liblogur/cmake 
stages:
  - build
  - upload
tags:
   - aws

build_bin_job:
  stage: build 
  script:
    - echo $CI_JOB_NAME
    - mkdir build
    - cd build
    - cmake ..
    - make
build_rpm_job:
  image: centos
  staege: build
  script:
    - ls -l
upload_job:
  stage: upload
  script:
    - cd build
